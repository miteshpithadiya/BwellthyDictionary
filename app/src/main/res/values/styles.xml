<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <!-- ToolBar -->
    <style name="ToolBarStyle" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>
        <!--<item name="actionOverflowButtonStyle">@style/ActionButtonOverflowStyle</item>-->
        <!--<item name="drawerArrowStyle">@style/DrawerArrowStyle</item>-->
    </style>

    <style name="avatar_with_text_icon_primary">
        <item name="android:paddingLeft">@dimen/avatar_three_line_text_text_padding_left</item>
        <item name="android:textSize">@dimen/avatar_three_line_text_font_size</item>
        <item name="android:textColor">@color/primary_text</item>
    </style>

    <style name="avatar_with_text_icon_secondary">
        <item name="android:paddingLeft">@dimen/avatar_three_line_text_text_padding_left</item>
        <item name="android:textSize">@dimen/avatar_three_line_text_secondary_font_size</item>
        <item name="android:textColor">@color/secondary_text</item>
        <item name="android:lines">2</item>
    </style>

    <style name="avatar_with_text_icon_avatar">
        <item name="android:paddingLeft">@dimen/avatar_three_line_text_padding_left</item>
    </style>

    <style name="text_label">
        <item name="android:textSize">16dp</item>
        <item name="android:textColor">@color/primary_text</item>
    </style>

</resources>
